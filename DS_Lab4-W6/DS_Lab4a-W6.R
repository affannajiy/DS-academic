#Factors are the for grouping and categorizing the value into groups, syntax same like a vector
#Value creation with the same data type
#Why?: Save memory because in binary environment 

blood_type <- c("A", "B", "AB", "O")

#Matrices will have a "recycling concept" to fill up the empty spaces

#Array can help you create multiple tables